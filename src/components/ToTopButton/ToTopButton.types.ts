import { AnyStyledComponent } from 'styled-components';

export interface IToTopButtonProps {
  idElement: string;
  reference: any;
  height: number;
  hoverColor?: string;
  className?: AnyStyledComponent;
}
